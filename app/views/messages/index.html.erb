<h1>Messages</h1>

<ul>
  <% @messages.each do |message| %>
    <li><%= link_to message.title, message %></li>
  <% end %>
</ul>

<%= form_for @message, url: {action: "create"}, html: {class: ""} do |f| %>
  <%= f.text_field :title %>
  <%= f.submit "Create" %>
<% end %>
